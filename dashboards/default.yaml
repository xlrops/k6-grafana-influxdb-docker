apiVersion: 1

providers:
  - name: dashboards # A uniquely identifiable name for the provider
    folder: K6 Tests # The folder where to place the dashboards
    folderUid: k6tests
    type: file
    options:
      path: /var/lib/grafana/dashboards/dashboard.json
    

